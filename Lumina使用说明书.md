# Lumina 使用说明书

## 1. 产品简介

Lumina 是一套用于 Windows 电脑的显示器辅助控制工具，由 Lumina 设备和 Windows 上位机程序组成。程序可读取设备方向和环境光数据，并根据用户配置控制显示器旋转、亮度、暗屏状态和设备 LED。

主要能力：

- 根据 Lumina 设备姿态自动旋转绑定显示器。
- 根据环境光变化自动调节显示器亮度。
- 支持多台 Lumina 设备分别绑定不同显示器。
- 支持多显示器亮度控制。
- 通过系统托盘面板进行绑定、亮度、档位、LED 和自启动等配置。
- 锁屏、休眠或睡眠前临时关闭已连接设备 LED，解锁或恢复后按配置恢复 LED 状态。

![产品整体示意图占位](assets/manual/product-overview.png)

图片建议：放置一张“Lumina 设备 + Windows 电脑 + 外接显示器”的整体连接示意图，标明设备连接、环境光感知和显示器控制三个方向。

## 2. 适用环境

### 2.1 系统要求

- 操作系统：Windows 10 或 Windows 11。
- 运行方式：推荐使用发布版 `Lumina.exe`。
- 显示器控制：部分外接显示器需要在显示器菜单中开启亮度控制相关选项。
- 设备连接：Lumina 设备通过 USB 连接电脑。

### 2.2 注意事项

- 部分显示器、扩展坞、转接线或显卡驱动可能不支持软件亮度控制。
- Windows 内置屏幕和外接显示器的亮度控制方式可能不同，实际效果以设备支持情况为准。
- 若移动了发布版文件夹位置，需要重新开启自启动。
- LED 控制需要 Lumina 固件支持对应的 HID 控制指令。

![使用环境示意图占位](assets/manual/environment.png)

图片建议：绘制电脑、Lumina 设备、外接显示器、USB 连接线、显示器视频线的连接关系图。

## 3. 安装与启动

### 3.1 使用发布版

1. 打开发布目录：

```text
dist_release\Lumina
```

2. 双击运行：

```text
Lumina.exe
```

3. 程序启动后会显示在 Windows 系统托盘区域。

运行发布版时需要保留整个 `Lumina` 文件夹，不要只复制 `Lumina.exe`。

![发布目录示意图占位](assets/manual/release-folder.png)

图片建议：放置 `dist_release\Lumina` 文件夹截图，框出 `Lumina.exe` 和必要的资源文件。

### 3.2 使用源码版

源码版适合开发、调试或验证功能。普通用户建议直接使用发布版。

```powershell
python -m pip install -r requirements.txt
python src\auto_dim_screen.py
```

![源码运行示意图占位](assets/manual/source-run.png)

图片建议：放置 PowerShell 运行命令截图，包含依赖安装和启动命令。

## 4. 托盘面板

程序运行后，会在 Windows 系统托盘区域显示 Lumina 图标。点击图标或在右键菜单中选择“亮度调节”可打开控制面板。面板会自动保存配置，不需要额外点击保存按钮。

托盘面板常用功能：

- 查看 Lumina 设备连接状态。
- 查看显示器列表和自动亮度绑定关系。
- 手动调节显示器亮度。
- 开启或关闭自动亮度。
- 开启或关闭自动旋转。
- 开启或关闭每台 Lumina 的 LED。
- 配置亮度档位。
- 设置自动暗屏空闲阈值。
- 开启或关闭开机自启动。
- 关闭面板或退出程序。

托盘菜单包含以下入口：

- `亮度调节`：打开托盘面板。
- `启用自动调光`：启用或暂停自动暗屏/自动调光逻辑。
- `立即恢复亮度`：立即恢复被自动暗屏降低的显示器亮度。
- `开启自启动` 和 `关闭自启动`：管理当前用户开机自启动。
- `退出`：退出 Lumina-host。

![托盘入口示意图占位](assets/manual/tray-entry.png)

图片建议：截取 Windows 任务栏右下角托盘图标，标出 Lumina 图标位置。

![托盘面板示意图占位](assets/manual/tray-panel.png)

图片建议：截取 Lumina 托盘面板主界面，标注 Lumina 设备卡、LED 红白圆点、显示器亮度区、底部自动暗屏按钮和自启动按钮。

## 5. 设备与显示器绑定

当电脑连接多台 Lumina 或多台显示器时，需要为每台 Lumina 指定自动旋转目标显示器，并为每台显示器选择自动亮度来源。当前面板分为上方 Lumina 设备卡和下方显示器亮度区。

显示器坐标轴以正对屏幕观察为准：横向从左到右为 `X+`，纵向从下到上为 `Y+`。在设备卡里设置 `屏幕X+` 和 `屏幕Y+` 时，就是告诉程序这两个显示器方向分别对应 Lumina 当前安装后的哪个机身方向。

Lumina 外壳边缘有坐标轴丝印，设置方向时可以直接对照丝印判断机身的 `X+`、`Y+` 等方向。

![显示器坐标轴示意图](assets/manual/display-coordinate-axis.svg)

### 5.1 绑定步骤

1. 确认 Lumina 设备已经通过 USB 连接电脑。
2. 打开 Lumina 托盘面板。
3. 在上方对应的 Lumina 设备卡中，通过显示器下拉框选择自动旋转目标显示器。
4. 在同一设备卡中设置 `屏幕X+` 和 `屏幕Y+` 对应的 Lumina 机身方向。
5. 在下方显示器亮度区，通过每行右侧下拉框选择 `手动` 或某台 Lumina。
6. 选择 `手动` 时可拖动该显示器亮度滑杆；选择 Lumina 时，该显示器交给对应设备自动亮度控制。
7. 改变 Lumina 设备方向或环境光，确认目标显示器响应正确。

![设备绑定流程图占位](assets/manual/device-binding-flow.png)

图片建议：绘制“选择设备卡显示器 -> 设置屏幕轴方向 -> 在显示器区选择亮度来源 -> 验证效果”的流程图。

![设备绑定界面示意图占位](assets/manual/device-binding-panel.png)

图片建议：截取设备卡和显示器亮度区，标注设备名称、显示器下拉框、屏幕轴方向下拉框和显示器亮度来源下拉框。

### 5.2 绑定建议

- 一台 Lumina 建议负责一台主要显示器的自动旋转。
- 多显示器环境下，建议先通过 Windows 显示设置确认显示器编号和物理位置。
- 若更换显示器、扩展坞或接口，建议重新检查绑定关系。
- 显示器亮度来源选为 `手动` 时不会被 Lumina 自动亮度接管。

## 6. 自动旋转

自动旋转功能会根据 Lumina 设备的姿态方向，自动调整绑定显示器的屏幕方向。

自动旋转使用显示器坐标轴作为基准：正对屏幕时，左到右为 `X+`，下到上为 `Y+`。例如设备卡中 `屏幕X+` 选择 `Y+`，表示显示器左到右的方向对应 Lumina 机身的 `Y+` 方向。

如果不确定 Lumina 机身方向，请查看设备外壳边缘的坐标轴丝印。

### 6.1 使用步骤

1. 将 Lumina 设备固定在目标显示器上。
2. 在对应设备卡中选择目标显示器。
3. 设置 `屏幕X+` 和 `屏幕Y+` 对应的 Lumina 机身方向。
4. 勾选设备卡中的 `自动旋转`。
5. 旋转显示器或 Lumina 设备，观察 Windows 显示方向是否同步变化。

![自动旋转安装示意图占位](assets/manual/auto-rotation-mount.png)

图片建议：放置 Lumina 设备固定在显示器背面或边框位置的照片，标明设备方向。

![自动旋转效果示意图占位](assets/manual/auto-rotation-effect.png)

图片建议：用左右对比图展示横屏和竖屏两种状态，标出 Lumina 姿态变化与显示器方向变化的对应关系。

### 6.2 调试建议

- 若旋转方向相反，检查 `屏幕X+` 和 `屏幕Y+` 方向配置是否与 Lumina 安装方向一致。
- 若系统没有响应，确认该显示器已正确绑定。
- 若 Windows 显示方向被系统策略锁定，需要先解除系统限制。

## 7. 自动亮度

自动亮度功能会读取 Lumina 感知到的环境光变化，并按照预设档位设置绑定显示器亮度。

### 7.1 亮度档位

亮度档位用于定义不同环境光条件下的显示器亮度。设备卡中固定显示 0 档到 4 档五个输入框，输入值为目标亮度百分比。例如：

| 环境光范围 | 建议亮度 |
| --- | --- |
| 较暗环境 | 20% - 35% |
| 普通室内 | 40% - 65% |
| 明亮环境 | 70% - 100% |

实际档位需要根据显示器亮度表现、办公环境和用户习惯调整。点击档位名称可以打开范围编辑浮窗，用于调整各档 lux 分界点。

![亮度档位配置示意图占位](assets/manual/brightness-levels.png)

图片建议：截取设备卡中的亮度档位区域，标注 0 档到 4 档输入框和档位范围编辑入口。

### 7.2 使用步骤

1. 确认 Lumina 设备连接正常。
2. 确认显示器支持亮度控制。
3. 在设备卡中配置 0 档到 4 档目标亮度百分比。
4. 点击档位名称，按需要调整 lux 分界点。
5. 勾选设备卡中的 `自动亮度`。
6. 在下方显示器亮度区，将目标显示器右侧下拉框选择为该 Lumina。
7. 改变环境光，观察显示器亮度是否按档位变化。

![自动亮度流程图占位](assets/manual/auto-brightness-flow.png)

图片建议：绘制“读取环境光 -> 匹配档位 -> 设置显示器亮度 -> 状态反馈”的流程图。

### 7.3 使用建议

- 档位范围不要设置过密，避免亮度频繁跳变。
- 建议在常用办公环境下测试并微调亮度百分比。
- 外接显示器需要确认亮度控制相关选项已开启。
- 如果显示器行右侧选择 `手动`，该显示器不会执行自动亮度。

## 8. 自动暗屏

自动暗屏用于在电脑空闲达到阈值后自动降低屏幕亮度，减少长时间无人使用时的亮度和功耗。

### 8.1 使用步骤

1. 打开 Lumina 托盘面板。
2. 在面板底部左侧输入框中设置空闲阈值，单位为秒。
3. 点击底部月亮按钮启用或暂停自动暗屏。
4. 如需立即恢复被降低的亮度，可在托盘菜单中点击 `立即恢复亮度`。
5. 等待电脑空闲达到阈值，观察显示器亮度是否自动降低。

![自动暗屏示意图占位](assets/manual/auto-dim.png)

图片建议：使用前后对比图展示普通亮度和暗屏亮度，并标出触发条件。

### 8.2 注意事项

- 自动暗屏不等同于关闭显示器。
- 若显示器最低亮度仍然偏亮，需要结合显示器自身菜单调整。
- 若暗屏影响正常使用，可暂停自动暗屏或提高空闲阈值。

## 9. 设备 LED 控制

每张 Lumina 设备卡右上角都有两个圆点：

- 红色圆点：关闭该 Lumina 的 LED。
- 白色圆点：打开该 Lumina 的 LED。

点击任一圆点会立即向对应设备发送一次 LED 控制指令，并把该状态写入配置。下次程序启动或设备重新连接后，上位机会按保存的状态重新下发一次 LED 指令。

当主机锁屏、休眠或进入睡眠前，程序会为每个已连接 Lumina 临时发送一次 LED 关闭指令。主机解锁或恢复后，程序会按配置恢复每台 Lumina 的 LED 状态。

![LED 控制示意图占位](assets/manual/led-control.png)

图片建议：截取设备卡右上角红色和白色圆点，标注“关闭 LED”和“打开 LED”。

## 10. 开机自启动

发布版可以通过面板底部右侧三角按钮或托盘菜单开启、关闭开机自启动。也可以使用以下脚本：

```powershell
powershell -ExecutionPolicy Bypass -File tools\install_autostart.ps1
powershell -ExecutionPolicy Bypass -File tools\uninstall_autostart.ps1
```

自启动写入当前用户注册表：

```text
HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Run
```

如果移动了 `Lumina` 文件夹位置，请重新运行程序并重新开启自启动。

![自启动设置示意图占位](assets/manual/autostart.png)

图片建议：截取托盘菜单中的“开机自启动”开关，或截取 Windows 启动项配置结果。

## 11. 日志与问题排查

### 11.1 日志位置

默认日志位置：

```text
dist_release\Lumina\logs\lumina.log
```

如果程序目录不可写，会自动回退到：

```text
%LOCALAPPDATA%\Lumina\logs\lumina.log
```

![日志文件示意图占位](assets/manual/log-file.png)

图片建议：放置日志目录截图，标出 `lumina.log` 文件。

### 11.2 常见问题

#### 找不到 Lumina 设备

处理建议：

1. 确认 USB 线连接正常。
2. 更换 USB 接口后重启 Lumina 程序。
3. 检查设备管理器中是否能看到 Lumina 设备。
4. 查看日志中是否存在设备枚举失败信息。

#### 外接显示器无法调节亮度

处理建议：

1. 打开显示器菜单，确认亮度控制相关选项已开启。
2. 尽量直接连接显示器，减少扩展坞或转接线影响。
3. 更新显卡驱动。
4. 查看日志中是否存在亮度控制失败信息。

#### 自动旋转无效

处理建议：

1. 确认 Lumina 已绑定到正确显示器。
2. 确认自动旋转开关已开启。
3. 检查 Windows 显示设置是否允许旋转。
4. 查看日志中是否有方向识别或显示器设置失败信息。

#### 亮度变化不符合预期

处理建议：

1. 检查亮度档位阈值是否设置合理。
2. 检查 Lumina 环境光窗口是否被遮挡。
3. 调整档位之间的间隔，避免频繁跳变。
4. 查看日志中的环境光数据和亮度设置记录。

#### LED 点击后没有反应

处理建议：

1. 确认 Lumina 固件已经支持 LED HID 控制指令。
2. 确认设备仍然处于 USB 连接状态。
3. 重新插拔设备后再次点击红色或白色圆点。
4. 查看日志中是否存在 HID Feature Report 下发失败记录。

![故障排查流程图占位](assets/manual/troubleshooting-flow.png)

图片建议：绘制“问题现象 -> 检查设备连接 -> 检查绑定配置 -> 检查显示器能力 -> 查看日志”的排查流程图。

## 12. 图片素材清单

建议后续补充以下图片到 `assets/manual/` 目录：

| 文件名 | 图片内容建议 |
| --- | --- |
| `product-overview.png` | Lumina 设备、电脑、显示器整体关系图 |
| `environment.png` | 使用环境和线缆连接示意图 |
| `release-folder.png` | 发布版文件夹截图 |
| `source-run.png` | 源码运行命令截图 |
| `tray-entry.png` | Windows 托盘入口截图 |
| `tray-panel.png` | Lumina 托盘面板主界面截图 |
| `display-coordinate-axis.svg` | 显示器 X+ 和 Y+ 坐标轴示意图 |
| `device-binding-flow.png` | 设备绑定流程图 |
| `device-binding-panel.png` | 设备绑定界面截图 |
| `auto-rotation-mount.png` | Lumina 安装方向照片或示意图 |
| `auto-rotation-effect.png` | 横屏/竖屏自动旋转效果对比图 |
| `brightness-levels.png` | 亮度档位配置界面截图 |
| `auto-brightness-flow.png` | 自动亮度工作流程图 |
| `auto-dim.png` | 自动暗屏效果对比图 |
| `led-control.png` | 设备卡 LED 红白圆点控制截图 |
| `autostart.png` | 自启动开关截图 |
| `log-file.png` | 日志文件位置截图 |
| `troubleshooting-flow.png` | 故障排查流程图 |

## 13. 版本记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| V1.0 | 2026-06-05 | 初版说明书，预留示意图位置和图片建议 |
| V1.1 | 2026-06-23 | 更新当前托盘面板操作、自动保存、LED 控制和自动暗屏说明 |
